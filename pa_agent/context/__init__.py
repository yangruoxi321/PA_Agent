"""多维上下文层：基本面/资金面/宏观/情绪，作为价格行为分析的附加信息。

本包只做"数据采集 + 渲染"，挂接到 prompt 组装器。设计原则：
- 纯附加，绝不破坏现有 K 线/价格行为主流程。
- 任何外部数据失败静默降级，永不向主流程抛异常。
- 单向依赖：context/ 可依赖 data/，data/ 不得反向依赖 context/。
"""

from __future__ import annotations
